export class Favoris {
    favId;
    favIdRes;
    fsUtilisateurByFavIdUtil;

    constructor(favId, favIdRes, fsUtilisateurByFavIdUtil) {
        this.favId = favId;
        this.favIdRes = favIdRes;
        this.fsUtilisateurByFavIdUtil = fsUtilisateurByFavIdUtil;
    }
}