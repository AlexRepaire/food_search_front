export class Fidelite {
    fidId;
    fidPtsFid;
    fsUtilisateurByFidIdUtil;

    constructor(fidId, fidPtsFid, fsUtilisateurByFidIdUtil) {
        this.fidId = fidId;
        this.fidPtsFid = fidPtsFid;
        this.fsUtilisateurByFidIdUtil = fsUtilisateurByFidIdUtil;
    }
}