export class JoursConges {
    id;
    jourConge;
    fSRestaurant;

    constructor(id, jourConge, fsRestaurant) {
        this.id = id;
        this.jourConge = jourConge;
        this.fsRestaurant = fsRestaurant;
    }
}